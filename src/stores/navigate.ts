import { defineStore } from 'pinia'

/**
 * 导航状态 store
 *
 * 阶段 2：仅创建空壳
 * 阶段 3 将填充：
 *   - state: inputBox / menuOptions / expandedCategory / navigateContent / highlightItems / drawer 状态
 *   - actions: 各字段 setter + 拉取导航数据
 *   - 持久化: 无
 */
export const useNavigateStore = defineStore('navigate', () => {
  return {}
})
